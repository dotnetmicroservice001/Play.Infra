apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: playeconomyapp-tls-cert
  namespace: emissary
spec:
  secretName: playeconomyapp-tls
  issuerRef: 
   name: letsencrypt-prod
   kind: ClusterIssuer
  dnsNames:
    - "gameplayeconomy.com"
    - "www.gameplayeconomy.com"